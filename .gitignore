# Build outputs
/build/
app/build/
*.apk
*.aab
*.dex

# Gradle cache
.gradle/

# Machine-specific SDK path — each developer sets their own in local.properties
local.properties

# IDE
.idea/
*.iml
.DS_Store

# Stale artefacts that should never be committed
old .git/

# Keystore files and secrets — NEVER commit these
*.jks
*.keystore
keystore.properties
